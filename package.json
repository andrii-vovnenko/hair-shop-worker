{
	"name": "hair-shop-worker",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy --minify",
		"migrate": "wrangler d1 migrations apply hair",
		"migrate:remote": "wrangler d1 migrations apply hair --remote",
		"cf-typegen": "wrangler types --env-interface CloudflareBindings"
	},
	"dependencies": {
		"@hono/swagger-ui": "^0.5.2",
		"hono": "^4.9.9",
		"uuid": "^11.1.0"
	},
	"devDependencies": {
		"wrangler": "^4.40.3"
	}
}
